"""
This script will eventually import a Ludo Sapiens trimesh into the current scene.
"""

import bpy

#-----------------------------------------------------------------------------
def load( operator, context, filepath ):
   print('\n\nimporting Ludo Sapiens trimesh %r' % filepath)
   print('Not yet supported...')
   return {'CANCELLED'}

   #scene = context.scene
   #obj   = context.object
   #if bpy.ops.object.mode_set.poll():
   #    bpy.ops.object.mode_set( mode='OBJECT' )
   #f = open( filepath, 'r' )
   # TODO: read lines.
   #return {'FINISHED'}
